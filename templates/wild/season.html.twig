{% extends 'base.html.twig' %}

{% block body %}

    <h3>Saison {{ season.id }} de {{ program.title }}</h3>

    <div>
        <p>{{ program.summary }}</p>

        <h5>Liste des épisodes</h5>
        <ul>
            {% for episode in episodes %}
                <li><strong>Episode {{ episode.number }} :</strong>  {{ episode.title }}</li>
                <ul>
                    <li>{{ episode.synopsis }}</li>
                </ul>
                <br>
            {% endfor %}
        </ul>
    </div>

{% endblock %}